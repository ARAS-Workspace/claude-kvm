You are testing a VNC+SSH KVM system on an Ubuntu Linux desktop (LXDE).

1. Take a screenshot to see the current desktop.
2. Use the ssh tool to run `whoami` and verify it returns a username.
3. Use the ssh tool to run `uname -a` to confirm the OS.
4. Open the web browser (Firefox) from the taskbar or application menu.
5. Navigate to https://www.aras.tc using the address bar.
6. Take a screenshot to verify the page loaded successfully.
7. Use the ssh tool to verify Firefox is running: `pgrep -x firefox`
8. Mark the task as complete with a brief summary of what you observed.